// Imports are commented out due to missing NestJS dependencies
// import { Module } from '@nestjs/common';
// import { ConfigModule, ConfigService } from '@nestjs/config';
// import { TypeOrmModule } from '@nestjs/typeorm';
// import { AuthModule } from './auth/auth.module';
// import { getTypeOrmConfig } from './config/typeorm.config';
// import { PrismaModule } from './prisma/prisma.module';
// import { TaxFormModule } from './tax-form/tax-form.module';

/**
 * Temporary AppModule implementation
 * This is a placeholder until NestJS dependencies are properly installed.
 * Replace this with the actual implementation when dependencies are available.
 */
export class AppModule {
  static imports = [
    /* ConfigModule, TypeOrmModule, PrismaModule, AuthModule, TaxFormModule */
  ];
} 