/**
 * This is a simplified app module to avoid decorator issues
 * We're temporarily bypassing NestJS and using Express directly
 * See server.js for the Express implementation
 */

export class AppModule {
  // This is just a placeholder class
  // The actual functionality is in server.js
} 